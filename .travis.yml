language: java

jdk: openjdk11

cache:
  directories:
    - $HOME/.m2

jobs:
  include:
    - stage: unit tests
      script: mvn clean test -DskipITs
    - stage: integration tests
      script: mvn clean integration-test -DskipUTs
server:
  port: 8071

spring:
  cloud:
    config:
      uri: http://localhost:8088
  transaction:
    default-timeout: 20

  datasource:
    hikari:
      minimumIdle: 5
      maximumPoolSize: 20
      idleTimeout: 10000
      maxLifetime: 2000000
      connectionTimeout: 10000